// //main greeting animation to get length of Text animation
// const mainGreetingWords = document.querySelectorAll("#mainGreetingWords path");
// // get the length to add in nth-child css
// for (let i = 0; i < mainGreetingWords.length; i++) {
//     console.log(`Letter${i} is ${mainGreetingWords[i].getTotalLength()}`);
// }
